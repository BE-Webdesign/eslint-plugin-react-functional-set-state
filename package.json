{
  "name": "eslint-plugin-react-functional-set-state",
  "version": "1.2.0",
  "description": "Lint rule for React, should use functional setState",
  "repository": {
    "type": "git",
    "url": "https://github.com/BE-Webdesign/eslint-plugin-react-functional-set-state.git"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "Edwin Cromley",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint ./",
    "lint:fix": "eslint ./ --fix",
    "test": "npm run lint && npm run unit-test",
    "unit-test": "istanbul cover --dir reports/coverage node_modules/mocha/bin/_mocha tests/**/*.js -- --reporter dot"
  },
  "dependencies": {
    "requireindex": "~1.1.0"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "^3.9.1",
    "istanbul": "0.4.5",
    "mocha": "^3.1.2"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "ISC"
}
